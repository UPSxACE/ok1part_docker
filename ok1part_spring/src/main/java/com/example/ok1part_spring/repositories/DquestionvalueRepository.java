package com.example.ok1part_spring.repositories;

import com.example.ok1part_spring.models.Dquestionvalue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DquestionvalueRepository extends JpaRepository<Dquestionvalue, Integer> {
}